-- This script is part of cfillion_Toggle take FX bypass for selected items.lua
-- @noindex

local dir = debug.getinfo(1, 'S').source:match("^@(.*[/\\])");
dofile(dir .. "cfillion_Toggle take FX bypass for selected items.lua")

toggleTakeFX(2)
